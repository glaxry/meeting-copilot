Store optional transcript sidecars here.

If `meeting.wav` is uploaded and `data/annotations/meeting.txt` exists, the service will use the text file as the transcript body and split it across the detected speech windows.
